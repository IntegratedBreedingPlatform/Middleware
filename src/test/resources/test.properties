# Middleware test configuration 

workbench.host =${test.middleware.workbench.host}
workbench.port =${test.middleware.workbench.port}
workbench.dbname =${test.middleware.workbench.dbname}
workbench.username =${test.middleware.workbench.username}
workbench.password =${test.middleware.workbench.password}
workbench.driver=${test.workbench.driver}
workbench.installation_dir=${test.workbench.installation_dir}

crop.host =${test.middleware.crop.host}
crop.port =${test.middleware.crop.port}
crop.dbname =${test.middleware.crop.dbname}
crop.username =${test.middleware.crop.username}
crop.password =${test.middleware.crop.password}
crop.db.url =${test.crop.db.url}
germplasm.edition.max.recursion=5
germplasm.grouping.max.recursion=${germplasm.grouping.max.recursion}

test.atomikos.transaction.timeout=${test.atomikos.transaction.timeout}

bms.version=17.1

export.lot.max.total.results=${export.lot.max.total.results}
cop.btype=0

feedback.enabled=true
